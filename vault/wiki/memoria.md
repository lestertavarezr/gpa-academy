# Memoria

`vault/raw/` — todo capturado, sin depurar.
`vault/wiki/` — conocimiento depurado, enlazado con `[[wikilinks]]` (este grafo).
`vault/outputs/` — todo lo que las skills entregan.

Ver [[sistema-jarvis]] y la skill `boveda` en `.claude/skills/boveda/SKILL.md`.
